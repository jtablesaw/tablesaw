package com.github.lwhite1.tablesaw;

/**
 *
 */
public class TestDataUtils  {

/*
  public static List<String> femaleFirstNames() {
    return new ArrayList<>();
  }

  @Test
  public static List<String> maleFirstNames() {
    ColumnType[] types = {TEXT, CATEGORY, SKIP};
    try {
      Table t = CsvReader.read(types, "data/first_names.csv");
      //View f = t.select().where(column()).run();
      System.out.println(t.columnNames());
    } catch (IOException e) {
      e.printStackTrace();
    }


    return new ArrayList<>();
  }

  public static List<String> lastNames() throws IOException {
    ColumnType[] types = {TEXT, SKIP, SKIP, SKIP, SKIP, SKIP, SKIP, SKIP, SKIP, SKIP, SKIP, SKIP};
    Table t = CsvReader.read(types, "data/last_names.csv");

    return new ArrayList<>();
  }

  public static List<List<String>> cityStateZip() {
    return null;
  }
*/
}
